kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  # Bind the API server to a host IP/port reachable by Argo CD
  apiServerAddress: "172.26.44.184"
  apiServerPort: 6443
nodes:
- role: control-plane
  extraPortMappings:
  # Optional: expose a NodePort range or ingress if you need later
  # - containerPort: 30080
  #   hostPort: 30080
  #   protocol: TCP